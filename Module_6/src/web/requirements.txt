# Web Framework
Flask==3.0.0

# Database Adapter (Binary version is faster to install in Docker)
psycopg2-binary==2.9.9

# RabbitMQ Client (Standard library for AMQP)
pika==1.3.2

# HTTP Library (Useful for making external API calls if needed)
requests==2.31.0